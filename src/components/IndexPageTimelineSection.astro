---
const props = Astro.props;
---

<section class="grid grid-cols-1 md:grid-cols-8 gap-4">
  <div class="col-span-2">
    <h2 class="mt-5 text-2xl font-display">{props.title ?? "Experiences"}</h2>
  </div>

  <div class="col-span-6 flex flex-col gap-3">
    <slot />
  </div>
</section>
